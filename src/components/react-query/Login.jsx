import { Link } from "react-router-dom";

const Login = () => {
    return (
        <>
            <p>This is login page</p>
            <Link to={'/'}>Back</Link>
        </>
    );
};

export default Login;